#
# Automatically generated by generate-makefiles.sh -- DO NOT EDIT.
#
# Restrictions for generate-makefiles.sh apply to this file. See COPYING for
# more details.
#
# Garrett Cooper, June 2010
#

# Path variables.
top_srcdir?=		../../..
subdir=			conformance/definitions/pthread_h
srcdir=			$(top_srcdir)/$(subdir)

prefix?=		/opt/openposix_testsuite
exec_prefix?=		$(prefix)
INSTALL_DIR=		$(DESTDIR)/$(exec_prefix)/$(subdir)
LOGFILE?=		logfile

# Build variables
CFLAGS+=		-I$(top_srcdir)/include

# XXX: for testfrmw.c -- needs to be moved into a library.
CFLAGS+=		-I$(srcdir)

# Top-level make definitions
CFLAGS+=		-std=c99 -D_POSIX_C_SOURCE=200112L -D_XOPEN_SOURCE=600 -W -Wall
LDFLAGS+=		
LDLIBS+=		
# Submake make definitions.
CFLAGS+=		
LDFLAGS+=		
LDLIBS+=		

INSTALL_TARGETS+=	
MAKE_TARGETS+=		pthread_h_3-10-buildonly.test pthread_h_18-1-buildonly.test pthread_h_3-8-buildonly.test pthread_h_19-1-buildonly.test pthread_h_3-5-buildonly.test pthread_h_20-1-buildonly.test pthread_h_10-buildonly.test pthread_h_3-4-buildonly.test pthread_h_9-1-buildonly.test pthread_h_15-1-buildonly.test pthread_h_13-1-buildonly.test pthread_h_8-1-buildonly.test pthread_h_3-11-buildonly.test pthread_h_3-1-buildonly.test pthread_h_2-2-buildonly.test pthread_h_3-6-buildonly.test pthread_h_3-2-buildonly.test pthread_h_1-1-buildonly.test pthread_h_6-1-buildonly.test pthread_h_12-1-buildonly.test pthread_h_3-13-buildonly.test pthread_h_3-7-buildonly.test pthread_h_5-1-buildonly.test pthread_h_17-1-buildonly.test pthread_h_11-1-buildonly.test pthread_h_4-1-buildonly.test pthread_h_16-1-buildonly.test pthread_h_3-3-buildonly.test pthread_h_7-1-buildonly.test pthread_h_2-1-buildonly.test pthread_h_3-12-buildonly.test pthread_h_14-1-buildonly.test pthread_h_3-9-buildonly.test

all: $(MAKE_TARGETS)
	@if [ -d speculative ]; then $(MAKE) -C speculative all; fi

clean:
	rm -f $(MAKE_TARGETS) logfile* run.sh *.core
	@if [ -d speculative ]; then $(MAKE) -C speculative clean; fi

install: $(INSTALL_DIR) run.sh
	set -e; for file in $(INSTALL_TARGETS) run.sh; do	\
		if [ -f "$$file" ] ; then			\
			install -m 00755 $$file		\
				$(INSTALL_DIR)/$$file;	\
		fi;						\
	done
	@if [ -d speculative ]; then $(MAKE) -C speculative install; fi

test: run.sh
	@./run.sh

$(INSTALL_DIR):
	mkdir -p $@

run.sh:
	@echo '#/bin/sh' > $@
	@echo "$(top_srcdir)/bin/run-tests.sh $(subdir) $(INSTALL_TARGETS)" >> $@
	@chmod +x run.sh

pthread_h_3-10-buildonly.test: $(srcdir)/3-10-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-10-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-10-buildonly compile PASSED"; \
		 echo "$(subdir)/3-10-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-10-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-10-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_18-1-buildonly.test: $(srcdir)/18-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/18-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/18-1-buildonly compile PASSED"; \
		 echo "$(subdir)/18-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/18-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/18-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-8-buildonly.test: $(srcdir)/3-8-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-8-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-8-buildonly compile PASSED"; \
		 echo "$(subdir)/3-8-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-8-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-8-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_19-1-buildonly.test: $(srcdir)/19-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/19-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/19-1-buildonly compile PASSED"; \
		 echo "$(subdir)/19-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/19-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/19-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-5-buildonly.test: $(srcdir)/3-5-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-5-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-5-buildonly compile PASSED"; \
		 echo "$(subdir)/3-5-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-5-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-5-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_20-1-buildonly.test: $(srcdir)/20-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/20-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/20-1-buildonly compile PASSED"; \
		 echo "$(subdir)/20-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/20-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/20-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_10-buildonly.test: $(srcdir)/10-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/10-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/10-buildonly compile PASSED"; \
		 echo "$(subdir)/10-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/10-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/10-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-4-buildonly.test: $(srcdir)/3-4-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-4-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-4-buildonly compile PASSED"; \
		 echo "$(subdir)/3-4-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-4-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-4-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_9-1-buildonly.test: $(srcdir)/9-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/9-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/9-1-buildonly compile PASSED"; \
		 echo "$(subdir)/9-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/9-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/9-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_15-1-buildonly.test: $(srcdir)/15-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/15-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/15-1-buildonly compile PASSED"; \
		 echo "$(subdir)/15-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/15-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/15-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_13-1-buildonly.test: $(srcdir)/13-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/13-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/13-1-buildonly compile PASSED"; \
		 echo "$(subdir)/13-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/13-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/13-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_8-1-buildonly.test: $(srcdir)/8-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/8-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/8-1-buildonly compile PASSED"; \
		 echo "$(subdir)/8-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/8-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/8-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-11-buildonly.test: $(srcdir)/3-11-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-11-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-11-buildonly compile PASSED"; \
		 echo "$(subdir)/3-11-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-11-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-11-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-1-buildonly.test: $(srcdir)/3-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-1-buildonly compile PASSED"; \
		 echo "$(subdir)/3-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_2-2-buildonly.test: $(srcdir)/2-2-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/2-2-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/2-2-buildonly compile PASSED"; \
		 echo "$(subdir)/2-2-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/2-2-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/2-2-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-6-buildonly.test: $(srcdir)/3-6-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-6-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-6-buildonly compile PASSED"; \
		 echo "$(subdir)/3-6-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-6-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-6-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-2-buildonly.test: $(srcdir)/3-2-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-2-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-2-buildonly compile PASSED"; \
		 echo "$(subdir)/3-2-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-2-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-2-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_1-1-buildonly.test: $(srcdir)/1-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/1-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/1-1-buildonly compile PASSED"; \
		 echo "$(subdir)/1-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/1-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/1-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_6-1-buildonly.test: $(srcdir)/6-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/6-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/6-1-buildonly compile PASSED"; \
		 echo "$(subdir)/6-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/6-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/6-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_12-1-buildonly.test: $(srcdir)/12-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/12-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/12-1-buildonly compile PASSED"; \
		 echo "$(subdir)/12-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/12-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/12-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-13-buildonly.test: $(srcdir)/3-13-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-13-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-13-buildonly compile PASSED"; \
		 echo "$(subdir)/3-13-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-13-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-13-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-7-buildonly.test: $(srcdir)/3-7-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-7-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-7-buildonly compile PASSED"; \
		 echo "$(subdir)/3-7-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-7-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-7-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_5-1-buildonly.test: $(srcdir)/5-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/5-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/5-1-buildonly compile PASSED"; \
		 echo "$(subdir)/5-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/5-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/5-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_17-1-buildonly.test: $(srcdir)/17-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/17-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/17-1-buildonly compile PASSED"; \
		 echo "$(subdir)/17-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/17-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/17-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_11-1-buildonly.test: $(srcdir)/11-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/11-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/11-1-buildonly compile PASSED"; \
		 echo "$(subdir)/11-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/11-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/11-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_4-1-buildonly.test: $(srcdir)/4-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/4-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/4-1-buildonly compile PASSED"; \
		 echo "$(subdir)/4-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/4-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/4-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_16-1-buildonly.test: $(srcdir)/16-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/16-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/16-1-buildonly compile PASSED"; \
		 echo "$(subdir)/16-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/16-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/16-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-3-buildonly.test: $(srcdir)/3-3-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-3-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-3-buildonly compile PASSED"; \
		 echo "$(subdir)/3-3-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-3-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-3-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_7-1-buildonly.test: $(srcdir)/7-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/7-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/7-1-buildonly compile PASSED"; \
		 echo "$(subdir)/7-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/7-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/7-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_2-1-buildonly.test: $(srcdir)/2-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/2-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/2-1-buildonly compile PASSED"; \
		 echo "$(subdir)/2-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/2-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/2-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-12-buildonly.test: $(srcdir)/3-12-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-12-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-12-buildonly compile PASSED"; \
		 echo "$(subdir)/3-12-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-12-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-12-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_14-1-buildonly.test: $(srcdir)/14-1-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/14-1-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/14-1-buildonly compile PASSED"; \
		 echo "$(subdir)/14-1-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/14-1-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/14-1-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

pthread_h_3-9-buildonly.test: $(srcdir)/3-9-buildonly.c
	@if $(CC) -c $(CFLAGS) $(LDFLAGS) -o $@ $(srcdir)/3-9-buildonly.c $(LDLIBS) > logfile.$$$$ 2>&1; then \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-9-buildonly compile PASSED"; \
		 echo "$(subdir)/3-9-buildonly compile PASSED" >> $(LOGFILE); \
	else \
		 cat logfile.$$$$; \
		 echo "$(subdir)/3-9-buildonly compile FAILED; SKIPPING"; \
		(echo "$(subdir)/3-9-buildonly compile FAILED; SKIPPING"; cat logfile.$$$$) >> $(LOGFILE); \
	fi; \
	rm -f logfile.$$$$

